# swift-executors Research Index

| Document | Topic | Date | Status |
|----------|-------|------|--------|
| [composable-executor-abstractions.md](composable-executor-abstractions.md) | Targeted analysis of how `IO.Event.Loop` / `IO.Completion.Loop` compose with `swift-executors` instead of hand-rolling `SerialExecutor` conformances. Recommends Design 1: a held `Polling.Executor` primitive. | 2026-04-15 | RECOMMENDATION (superseded in scope by `executor-package-design.md`; findings preserved as the Design-1 origin) |
| [executor-package-design.md](executor-package-design.md) | Complete-toolkit taxonomy: `swift-executor-primitives` (L1) + seven named compositions in `swift-executors` (L3). Validates V1–V8; post-supervision decisions recorded. | 2026-04-15 | DECISION |
| [executor-main-platform-architecture.md](executor-main-platform-architecture.md) | Four-revision architectural journey (R1→R4) for `Executor.Main` platform variation. Locks R4: witness-struct dependency inversion, universal scope with `MainActor` coexistence, Apple frameworks as L2 spec-mirror per [PLAT-ARCH-012], `Executor.MainThread` nested global actor per [API-NAME-001]. Documents R1's "conflating current-code-with-architectural-need" error and R2's "unexamined-premise" error as future-maintainer guidance. | 2026-04-16 | DECISION |
| [sync-handoff-to-actors.md](sync-handoff-to-actors.md) | Trade-off analysis of four sync→actor delivery mechanisms (`Task`, `Task.immediate`, direct `enqueue(UnownedJob)`, channel-based). Recommends channel-based handoff for fire-and-forget; `Task.immediate` reserved for messages needing per-task cancellation/priority; direct `enqueue` is unavailable to user code. | 2026-04-16 | DECISION |
| [work-stealing-scheduler-design.md](work-stealing-scheduler-design.md) | Lock the design for `Kernel.Thread.Executor.Stealing`: deque ABI (Chase-Lev vs. THE vs. Idempotent vs. private deques), steal-victim selection (random/nearest-neighbour/hierarchical), idle policy (spin/yield/park hysteresis), pool sizing, and actor-isolation interaction. Locked: Chase-Lev (Lê et al. 2013 corrected) + random victim selection (XorShift32 per-worker) + spin→yield→park + `TaskExecutor`-only conformance. Deque implemented in `swift-executor-primitives`; all 7 pre-DECISION steps completed. | 2026-04-16 | DECISION |
| [completion-executor-composition.md](completion-executor-composition.md) | Add `Kernel.Thread.Executor.Completion` as a sibling to `Polling` — accepting the unified `Kernel.Completion` primitive, absorbing the executor-generic machinery currently in `IO.Completion.Loop` — so IO Completions collapses to a single public `IO.Completion.Actor` (mirroring `IO.Event.Actor` + `IO.Blocking`, no struct wrapper, no internal actor, no Loop class). Decisions locked: name `Completion` (mechanism word, symmetric to `Polling`; "Proactor" rejected as a pattern name); one bundled commit; coroutine `_read`/`_modify` `kernel` accessor; `maxCompletionsPerPoll` default 256. Executor is backend-neutral (no io_uring / IOCP knowledge); single-thunk tick symmetric with Polling; proactor phase ordering (flush → drain → maybe-wait → drain) encapsulated inside runLoop. Net LOC across packages ≈ −70. Tier 2. | 2026-04-16 | DECISION |
| [priority-escalation-policy.md](priority-escalation-policy.md) | Per-executor policy for `TaskPriority` escalation across `Kernel.Thread.Executor.{Polling,Sharded,Stealing}`, `Executor.{Scheduled,Cooperative,Main}`. Four mechanisms: M1 enqueue-reorder, M2 drain-select, M3 thread-QoS bump, M4 aux stealer. Decision: M3-only, Darwin-only, opt-in via `Options.priorityTracking: Bool = false`; M1/M2 rejected (no payoff without M3); M4 rejected (our executors *are* the pool). Applies Sha-Rajkumar-Lehoczky 1990 PIP bound; cites `TaskStatus.cpp:1050` and `Actor.cpp:1632–2174` TODOs. Darwin M3 spike validated (853 ns/cycle, sub-µs, non-LIFO teardown OK, parked-worker non-wake confirmed). Implementation follow-up: wire bracketed override into drain paths when `priorityTracking` is true. Tier 2. | 2026-04-16 | DECISION |
| [scheduled-executor-policy.md](scheduled-executor-policy.md) | Lock the v1 policy for `Executor.Scheduled<Base>` along five axes: dispatch policy (EDF-dispatch, not Liu–Layland EDF scheduling); `SchedulingExecutor` conformance (conform — blocked by macOS 26.4 SDK `.swiftinterface` gap, template locked); clock generality (two hardcoded clocks for v1, spike validated); tolerance (`C.Duration?` per libdispatch leeway lineage); same-deadline tiebreaker (FIFO via monotonic sequencer, Java STPE pattern — L1 Entry gained sequence field, Priority tracks `_nextSequence`). Two-clock spike (4 variants, all PASS) confirmed shared-flag + per-clock-condvar-broadcast pattern. Rename-migration strategy locked: `hasFeature` + `compiler`-version double-gated conformance with separate arms for `SchedulingExecutor` and `SchedulableExecutor` names. Cites Liu & Layland 1973 verbatim. Tier 2. | 2026-04-16 | DECISION |
| [embedded-swift-scoping.md](embedded-swift-scoping.md) | Per-composition Embedded Swift viability for the v1 taxonomy. Verdicts locked: Cooperative=SHIP-WITH-GUARDS (only viable Embedded executor; matches stdlib CooperativeExecutor on wasm32); base/Sharded/Stealing=CONDITIONAL (deferred to RTOS Kernel.Thread backend); Polling/Completion/Main/Scheduled=NO (host-OS-only or ContinuousClock-blocked). TaskExecutor conformances gated `#if !$Embedded`. SchedulingExecutor conformance inherently `#if !$Embedded` (stdlib gates the protocol's generic overloads). Cites @_unavailableInEmbedded enumeration, Synchronization.Mutex absence on non-wasm Embedded, KERNEL_AVAILABLE exclude, and forum posts (McCall, Varma, Desiatov). Implementation follow-ups (TaskExecutor guard sweep, Embedded Wait backend, RTOS Kernel.Thread backend) deferred to when Package.swift adds Embedded platform + stdlib Embedded concurrency stabilizes. Tier 2. | 2026-04-16 | DECISION |
| [cooperative-donation-contract.md](cooperative-donation-contract.md) | Thread-donation contract for `Executor.Cooperative` along five axes. Decision: snapshot-then-check yield, `runUntil`+`stop()` revocation, always-returns guarantee, caller-owned priority, FIFO drain. Implemented on both Cooperative and Main non-Darwin. `RunLoopExecutor`/`SchedulingExecutor` conformances blocked: protocols absent from macOS 26.4 SDK `.swiftinterface`. Methods match protocol signatures; conformances are one-line additions when protocols ship. | 2026-04-16 | DECISION |
| [executor-identity-sharded.md](executor-identity-sharded.md) | `isIsolatingCurrentContext()` for N-thread `Kernel.Thread.Executor.Sharded`. Decision: iterate-shards for v1 (O(N) where N ≤ 16 ≈ 16 ns); atomic-ID-in-TLS as v2 target. Implemented in `Kernel.Thread.Executor.Sharded.swift`; tests cover shard-thread, non-shard-thread, and post-shutdown paths. | 2026-04-16 | DECISION |
| [polling-executor-queue-design.md](polling-executor-queue-design.md) | Lock-free Vyukov MPSC vs. current `Kernel.Thread.Mutex`-protected `Executor.Job.Queue` for `Kernel.Thread.Executor.Polling.enqueue(_:)`. Decision: keep locked queue for v1. Mutex is not the bottleneck (`epoll_wait` dominates), swap-drain is O(1) and cache-friendly, Tokio/Go/ForkJoinPool all use lock-protected injection. v2 trigger: benchmark demonstrating ≥ 50 ns P99 enqueue latency under production workload. | 2026-04-16 | DECISION |
| [numa-aware-sharding.md](numa-aware-sharding.md) | Cache-line padding + NUMA pinning + topology-aware steal-victim selection for Sharded and Stealing. Apple Silicon cache line: 128 bytes (verified via `sysctl`). Defined `CPU.Cache.Padded<T>` at L1 `swift-cpu-primitives` (first cache-line-aware type in the ecosystem) using `UnsafeMutableRawPointer.allocate(alignment: 128)`; supports `T: ~Copyable` for `Atomic<V>`/`Mutex<V>`; 8 tests pass. Applied to `Sharded.cursor` — `private let cursor: CPU.Cache.Padded<Atomic<Index<Kernel.Thread>>>`, access via `cursor.value.advance(within: count)`. macOS has no NUMA (UMA); L2 cluster is the locality boundary. Stealing's class-per-worker is accidentally safe (192-byte Worker instances). NUMA pinning and topology-aware stealing deferred to v2 via `Options` parameters. Tier 2. | 2026-04-16 | DECISION |

## Proposed Research

Drafts surfaced from cross-cutting analysis of the existing toolkit, the Swift runtime (`/Users/coen/Developer/swiftlang/swift`), and academic scheduling literature. Filenames are reserved; documents are not yet written. Status `DRAFT` indicates an identified question; convert to `IN_PROGRESS` when a research document begins.

| Document (proposed) | Topic | Date | Status |
|---------------------|-------|------|--------|
