# swift-executors Research Index

| Document | Topic | Date | Status |
|----------|-------|------|--------|
| [composable-executor-abstractions.md](composable-executor-abstractions.md) | Targeted analysis of how `IO.Event.Loop` / `IO.Completion.Loop` compose with `swift-executors` instead of hand-rolling `SerialExecutor` conformances. Recommends Design 1: a held `Polling.Executor` primitive. | 2026-04-15 | RECOMMENDATION (superseded in scope by `executor-package-design.md`; findings preserved as the Design-1 origin) |
| [executor-package-design.md](executor-package-design.md) | Complete-toolkit taxonomy: `swift-executor-primitives` (L1) + seven named compositions in `swift-executors` (L3). Validates V1–V8; post-supervision decisions recorded. | 2026-04-15 | DECISION |
| [executor-main-platform-architecture.md](executor-main-platform-architecture.md) | Four-revision architectural journey (R1→R4) for `Executor.Main` platform variation. Locks R4: witness-struct dependency inversion, universal scope with `MainActor` coexistence, Apple frameworks as L2 spec-mirror per [PLAT-ARCH-012], `Executor.MainThread` nested global actor per [API-NAME-001]. Documents R1's "conflating current-code-with-architectural-need" error and R2's "unexamined-premise" error as future-maintainer guidance. | 2026-04-16 | DECISION |
| [sync-handoff-to-actors.md](sync-handoff-to-actors.md) | Trade-off analysis of four sync→actor delivery mechanisms (`Task`, `Task.immediate`, direct `enqueue(UnownedJob)`, channel-based). Recommends channel-based handoff for fire-and-forget; `Task.immediate` reserved for messages needing per-task cancellation/priority; direct `enqueue` is unavailable to user code. | 2026-04-16 | DECISION |
| [work-stealing-scheduler-design.md](work-stealing-scheduler-design.md) | Lock the design for `Kernel.Thread.Executor.Stealing`: deque ABI (Chase-Lev vs. THE vs. Idempotent vs. private deques), steal-victim selection (random/nearest-neighbour/hierarchical), idle policy (spin/yield/park hysteresis), pool sizing, and actor-isolation interaction. Initial recommendations: Chase-Lev (Lê et al. 2013 corrected) + random victim selection + spin→yield→park + `TaskExecutor`-only conformance. Tier 2; awaiting deque spike in `swift-executor-primitives` and coordination with priority-escalation / NUMA / Embedded notes. | 2026-04-16 | IN_PROGRESS |
| [completion-executor-composition.md](completion-executor-composition.md) | Add `Kernel.Thread.Executor.Completion` as a sibling to `Polling` — accepting the unified `Kernel.Completion` primitive, absorbing the executor-generic machinery currently in `IO.Completion.Loop` — so IO Completions collapses to a single public `IO.Completion.Actor` (mirroring `IO.Event.Actor` + `IO.Blocking`, no struct wrapper, no internal actor, no Loop class). Decisions locked: name `Completion` (mechanism word, symmetric to `Polling`; "Proactor" rejected as a pattern name); one bundled commit; coroutine `_read`/`_modify` `kernel` accessor; `maxCompletionsPerPoll` default 256. Executor is backend-neutral (no io_uring / IOCP knowledge); single-thunk tick symmetric with Polling; proactor phase ordering (flush → drain → maybe-wait → drain) encapsulated inside runLoop. Net LOC across packages ≈ −70. Tier 2. | 2026-04-16 | DECISION |

## Proposed Research

Drafts surfaced from cross-cutting analysis of the existing toolkit, the Swift runtime (`/Users/coen/Developer/swiftlang/swift`), and academic scheduling literature. Filenames are reserved; documents are not yet written. Status `DRAFT` indicates an identified question; convert to `IN_PROGRESS` when a research document begins.

| Document (proposed) | Topic | Date | Status |
|---------------------|-------|------|--------|
| `priority-escalation-policy.md` | Priority-escalation policy across `Kernel.Thread.Executor.{Sharded,Stealing,Polling}`: OS thread priority bump (per `b30f63530a6` in stdlib) vs. job re-enqueue at higher priority vs. dedicated stealer jobs. Priority inheritance / priority ceiling protocols (Sha, Rajkumar, Lehoczky 1990) to bound priority inversion. Stdlib has unresolved stealer-on-bump TODO at `Actor.cpp:1632, 1638, 1723, 2147`. | 2026-04-16 | DRAFT |
| `scheduled-executor-policy.md` | Policy parameterization for `Executor.Scheduled<Base>`: EDF (Liu & Layland 1973) vs. Rate-Monotonic vs. fixed-priority dispatch; whether to conform to the experimental `SchedulingExecutor` SPI (`Executor.swift:559–624`, `@_spi(ExperimentalCustomExecutors)`) or stay independent. The SPI is moving; conformance cost vs. divergence cost is the trade-off. | 2026-04-16 | DRAFT |
| `executor-identity-sharded.md` | `isIsolatingCurrentContext()` and `complexEquality` story for `Kernel.Thread.Executor.Sharded`, where any of N threads is a valid "we're isolated" answer. Existing single-thread executors (`Polling`, base `Executor`) check `threadHandle?.isCurrent`; sharded extension is non-trivial. Stdlib churn at `05f98b2e994` and `8fbf0e07f38` shows the complex-equality surface is unstable. | 2026-04-16 | DRAFT |
| `embedded-swift-scoping.md` | Which of the seven compositions can ship to Embedded Swift; map dependencies on allocator, Dispatch, and `@_unavailableInEmbedded` runtime APIs (`Executor.swift:712–756`). Scoping note to prevent late surprises when the Embedded executor story stabilises. | 2026-04-16 | DRAFT |
| `cooperative-donation-contract.md` | Thread-donation contract for `Executor.Cooperative.runUntil`: caller-yield policy, donation revocation, completion guarantees. Stdlib's `CooperativeGlobalExecutor.cpp:250` carries the unresolved "should the donator have some say in this?" TODO; recent fixes (`bd27a14ea00`, `0fbd382e9ca`) shaped semantics but did not lock the contract. | 2026-04-16 | DRAFT |
| `polling-executor-queue-design.md` | Lock-free MPSC queue (Vyukov 2010, LMAX Disruptor) vs. current `Kernel.Thread.Mutex`-protected `Executor.Job.Queue` for `Kernel.Thread.Executor.Polling.enqueue(_:)`. The polling executor is single-consumer by construction, which is the precondition for MPSC. Establish benchmark methodology and contention thresholds before committing. | 2026-04-16 | DRAFT |
| `numa-aware-sharding.md` | Cache-line padding for per-shard queues and NUMA pinning policy for `Kernel.Thread.Executor.Sharded`; locality-aware steal selection as the Stealing counterpart. Currently round-robin shard selection ignores topology. Relevant on multi-socket Linux servers; minor on macOS. | 2026-04-16 | DRAFT |
